#include "oaz_level.h"

namespace oaz {
    /*
    Level::Level() {
        std::ifstream f("level.json");
        nlohmann::json jsonLevelMetaData = nlohmann::json::parse(f);
        //jsontype::lightActorMetaData actorMetaDataList[MAX_ACTOR_PER_LEVEL] = jsonLevelMetaData["actors"].get<jsontype::lightActorMetaData>();
        for(auto& element : jsonLevelMetaData["actors"]) {
            spdlog::info("name: {0}, uuid: {1}", element["name"], element["uuid"]);
        }
    }*/
}